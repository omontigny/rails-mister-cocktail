<!-- app/views/cocktails/show.html.erb -->
<div class="container">
      <h1>My Cocktail <strong><%= @cocktail.name %></strong></h1>
      <br>

       <div class="card-product">
        <%= image_tag "#{@cocktail.name}", alt: "alttext" %>
          <div class="card-product-infos">
              <% @cocktail.doses.each do | dose | %>
              <p><%= dose.ingredient.name.capitalize %> (<%= dose.description %>)
                <%= link_to dose_path(dose), method: :delete, class: 'remove-link', data: {confirm: 'Are you sure you want to remove this plant from the garden?'} do %>
                 <i class="fas fa-trash-alt"></i></p>
                <% end %>
              <% end %>
          </div>
          </div>

      <div class="bottom">
      <%= link_to "Add Ingredient", new_cocktail_dose_path(@cocktail), {class: "btn btn-flat"} %>
      <%= link_to "back", cocktails_path, {class: "btn btn-gradient"} %>
      </div>
    </div>


<!-- # Show all ingredients of this cocktail
# SELECT description,ingredient.name
# FROM doses
# JOIN doses.ingredient_id = ingredient.id
# WHERE doses.cocktail_id = cocktail.id -->







